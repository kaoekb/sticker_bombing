stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - docker build --no-cache -t duck_bot:$CI_COMMIT_SHA .

test:
  stage: test
  script:
    echo "test"
    

deploy:
  stage: deploy
  script:
    - docker-compose -f docker-compose.yml up --build -d
