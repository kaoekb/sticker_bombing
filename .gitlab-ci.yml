stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - docker build --no-cache -t duck_bot .

test:
  stage: test
  script:
    echo "test"
  #   - pip install -r requirements.txt
  #   # - pytest tests  # если у вас есть папка с тестами
  # only:
  #   - main
  #   - dev

deploy:
  stage: deploy
  script:
    - docker-compose -f docker-compose.yml up -d
