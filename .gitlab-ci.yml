stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    docker build -t sticker_bombing .

test-job:
  stage: test
  script:
    echo "test"

deploy-job:
  stage: deploy
  script:
    docker-compose up -d
